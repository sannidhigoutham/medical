const db= require("../db")

const signup = (data, callback)=>{
  db.query('SELECT * FROM user where name= ?', data, callback);
}

const signin = (data, callback)=>{
  db.query('SELECT * FROM user where name ? and password ?', [data.name,data.password], callback);
}

module.exports ={
    signup,
    signin
}